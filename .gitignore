# 의존성 · 빌드 산출물
node_modules/
web/node_modules/
dist/
web/dist/
coverage/
web/coverage/

# 파이썬
__pycache__/
*.pyc
.venv/

# OS · 에디터
.DS_Store
*.log

# 런타임 수집 데이터(개인/의료 라벨 데이터일 수 있어 커밋 금지)
feedback/*.jsonl
feedback/images/*
webjobs/

# 피드백 관리자(ADMIN_PASSWORD)를 로컬에 둘 때 쓰는 파일. 대화·저장소에 비번을
# 남기지 않기 위한 것. 공용 보관함은 2026-08-31부터 비번이 없어 이 파일을 쓰지
# 않는다(관리자 조회 /api/admin 에만 해당).
private/.library-password

# 생성 산출물·비공개 케이스(실물 라벨 이미지)는 저장소에 넣지 않는다
bench/out/
# 합성 케이스 이미지 캐시(레시피에서 언제든 재생성된다)
bench/work/
private/bench-cases/
# 단, 안전망의 계약·기준선·이력은 반드시 커밋한다(코드와 함께 버전 관리돼야
# 회귀를 커밋에 붙일 수 있다): bench/cases/, bench/baseline.json,
# bench/history.jsonl
