"""정확도 회귀 안전망(guard) — 엔진을 고칠 때 잘 되던 케이스가 죽는 것을 막는다.

설계는 두 층이다(자세한 배경은 README "정확도 안전망" 절).
  1층 계약(라벨)  — 사람이 확인한 사실. 반드시 잡아야 할 영역(must_find),
                    절대 뜨면 안 되는 영역(forbid), 오탐 예산(fp_budget).
                    깨지면 FAIL — 머지 불가.
  2층 표류(drift) — 라벨이 없는 영역의 신규·소실 검출과 마진 감소.
                    WARN — 사람이 판정한 뒤 1층 라벨로 승격한다.

`python -m bench.run` 으로 실행한다.
"""
